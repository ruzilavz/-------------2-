@echo off
REM Переходим в папку проекта (если .bat находится в другом месте):
REM cd C:\path\to\my\project

REM Запускаем встроенный сервер Python в отдельном окне (не блокируя скрипт):
start "" python -m http.server 8000 --bind 127.0.0.1

REM (Опционально) Небольшая пауза, чтобы сервер успел запуститься:
timeout /T 2 > NUL

REM Открываем браузер с локальным сайтом (откроется в браузере по умолчанию):
start "" http://localhost:8000
